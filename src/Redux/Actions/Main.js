import {
  GET_USER,
  GET_QUOTES,
  GET_NOTES,
  SAVE_NOTE,
} from 'constants/ActionTypes';

export const get_user = {
  type: GET_USER,
};

export const get_quotes = {
  type: GET_QUOTES,
};

export const save_note = {
  type: SAVE_NOTE,
};
